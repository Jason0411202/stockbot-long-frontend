# 後端 API 的完整 origin（含協定）。前端以此為 base URL 直連後端。
#
# 本機開發：指向本機 go run 起的後端（HTTP, :8080）
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
#
# 正式環境（Vercel）：請於 Vercel 專案的 Environment Variables 設定，
# 並使用 HTTPS 網域（由後端 Caddy 提供）以避免瀏覽器 mixed-content 阻擋：
# NEXT_PUBLIC_BACKEND_URL=https://your-backend-domain.com
